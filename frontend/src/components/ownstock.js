import React from 'react';
import firebase from '../firebase';

export default class StocksOwned extends React.Component {
    state = {

    }

    render(){
        return <div>stocks that the user owns</div>
    }
}