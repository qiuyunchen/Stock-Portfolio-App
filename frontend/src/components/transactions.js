import React from 'react';
import firebase from '../firebase';

export default class Transactions extends React.Component {
    state = {

    }

    render(){
        return <div>past transactions</div>
    }
}